# HealthConnect - Python Dependencies
# Flask-based healthcare web application with SQLite database

# Core Framework
Flask==3.0.0
Werkzeug==3.0.1

# Flask Dependencies (auto-installed with Flask)
# - Jinja2>=3.1.2 (templating engine)
# - itsdangerous>=2.1.2 (session security)
# - click>=8.1.3 (CLI support)
# - blinker>=1.6.2 (signals support)
# - MarkupSafe>=2.1.1 (string escaping)

# Database
# - SQLite3 (built into Python, no installation required)

